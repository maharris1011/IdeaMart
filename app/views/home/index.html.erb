<section class='dark-blue-background'>
<div class='jumbotron'>
	<div class='container'>
		<h1>All our Ideas.  All the time.</h1>
		<h2>Want to learn how it works?</h2>
		<a href='#' class='btn btn-primary btn-lg'>Learn More</a>
	</div>
</div>
</section>

<section class='light-background'>
<% if user_signed_in? then %>
	<%= render :partial => 'idealist' %>
<% else %> 
	<%= render :partial => 'benefits' %>
<% end %>
</section>